"""
Compute paired within-site and transportability performance differences.

The functions compare prediction outputs generated by the analysis pipeline
using paired stratified bootstrap confidence intervals.
"""

from pathlib import Path
from typing import Dict, Tuple

import numpy as np
import pandas as pd
from sklearn.metrics import brier_score_loss, roc_auc_score


# =============================================================================
# Standard pipeline labels
# =============================================================================

PRIMARY_DATASET_NAME = "primary_heldout"
LEAVE_SITE_OUT_DATASET_NAME = "leave_site_out"

FL_MODEL_TYPE = "fl_site"
CENTRAL_MODEL_TYPE = "central"
LOCAL_MODEL_TYPE = "local"

LSO_FL_MODEL_TYPE = "fl_site_leave_site_out"
LSO_CENTRAL_MODEL_TYPE = "central_leave_site_out"


# =============================================================================
# Basic helpers
# =============================================================================

def _as_1d_binary_array(x) -> np.ndarray:
    arr = np.asarray(x).reshape(-1)

    return (
        pd.to_numeric(pd.Series(arr), errors="raise")
        .astype(int)
        .to_numpy()
    )


def _as_1d_float_array(x) -> np.ndarray:
    arr = np.asarray(x).reshape(-1)

    return (
        pd.to_numeric(pd.Series(arr), errors="raise")
        .astype(float)
        .to_numpy()
    )


def _site_sort_key(x):
    text = str(x)

    if text.isdigit():
        return int(text)

    return text


def _get_site_column(metrics_df: pd.DataFrame) -> str:
    if "site_id" in metrics_df.columns:
        return "site_id"

    if "excluded_site_id" in metrics_df.columns:
        return "excluded_site_id"

    raise ValueError(
        "Metrics dataframe must contain either "
        "'site_id' or 'excluded_site_id'."
    )


def _resolve_prediction_path(
    prediction_csv_value,
    metrics_csv: Path,
) -> Path:
    path = Path(str(prediction_csv_value))

    if path.is_absolute():
        return path

    return Path(metrics_csv).parent / path


def _read_prediction_csv(
    path: Path,
) -> Tuple[np.ndarray, np.ndarray]:
    path = Path(path)

    if not path.exists():
        raise FileNotFoundError(
            f"Prediction CSV not found: {path}"
        )

    df = pd.read_csv(path)

    if "pred_prob" not in df.columns:
        raise ValueError(
            f"'pred_prob' column not found in prediction CSV: {path}"
        )

    y_true = _as_1d_binary_array(
        df.iloc[:, 0]
    )

    pred_prob = _as_1d_float_array(
        df["pred_prob"]
    )

    return y_true, pred_prob


def _get_prediction_for_site_model(
    *,
    metrics_df: pd.DataFrame,
    metrics_csv: Path,
    site_id,
    model_type: str,
    dataset_name: str,
) -> Tuple[np.ndarray, np.ndarray, Path]:
    site_col = _get_site_column(
        metrics_df
    )

    required_cols = {
        site_col,
        "model_type",
        "dataset",
        "prediction_csv",
    }

    missing_cols = (
        required_cols
        - set(metrics_df.columns)
    )

    if missing_cols:
        raise ValueError(
            "Metrics dataframe is missing required columns: "
            f"{sorted(missing_cols)}"
        )

    subset = metrics_df[
        (
            metrics_df[site_col]
            .astype(str)
            .eq(str(site_id))
        )
        & (
            metrics_df["model_type"]
            .astype(str)
            .eq(str(model_type))
        )
        & (
            metrics_df["dataset"]
            .astype(str)
            .eq(str(dataset_name))
        )
    ].copy()

    if subset.empty:
        available = (
            metrics_df[
                [
                    site_col,
                    "model_type",
                    "dataset",
                ]
            ]
            .drop_duplicates()
            .sort_values(
                [
                    site_col,
                    "model_type",
                    "dataset",
                ]
            )
        )

        raise ValueError(
            f"No row found for site={site_id}, "
            f"model_type={model_type}, "
            f"dataset={dataset_name} in {metrics_csv}.\n\n"
            f"Available combinations:\n"
            f"{available.to_string(index=False)}"
        )

    if len(subset) > 1:
        raise ValueError(
            f"Multiple rows found for site={site_id}, "
            f"model_type={model_type}, "
            f"dataset={dataset_name} in {metrics_csv}."
        )

    prediction_path = _resolve_prediction_path(
        subset.iloc[0]["prediction_csv"],
        metrics_csv,
    )

    y_true, pred_prob = _read_prediction_csv(
        prediction_path
    )

    return (
        y_true,
        pred_prob,
        prediction_path,
    )


def _check_same_patients(
    y_first: np.ndarray,
    y_second: np.ndarray,
    context: str,
) -> None:
    if len(y_first) != len(y_second):
        raise ValueError(
            f"Prediction lengths differ for {context}: "
            f"{len(y_first)} vs {len(y_second)}."
        )

    if not np.array_equal(
        y_first,
        y_second,
    ):
        raise ValueError(
            f"Outcome vectors differ for {context}. "
            "The comparison is not paired by row order."
        )


# =============================================================================
# Metric difference and paired bootstrap
# =============================================================================

def paired_metric_difference(
    y_true: np.ndarray,
    p_first: np.ndarray,
    p_second: np.ndarray,
    metric: str,
) -> float:
    """
    Compute metric(first strategy) - metric(second strategy).
    """
    metric = str(metric).lower()

    if metric == "auc":
        if len(np.unique(y_true)) < 2:
            return float("nan")

        return float(
            roc_auc_score(
                y_true,
                p_first,
            )
            - roc_auc_score(
                y_true,
                p_second,
            )
        )

    if metric == "brier":
        return float(
            brier_score_loss(
                y_true,
                p_first,
            )
            - brier_score_loss(
                y_true,
                p_second,
            )
        )

    raise ValueError(
        f"Unsupported metric: {metric}"
    )


def paired_stratified_bootstrap_difference_ci(
    y_true: np.ndarray,
    p_first: np.ndarray,
    p_second: np.ndarray,
    *,
    metric: str,
    n_boot: int = 1000,
    ci_level: float = 0.95,
    random_state: int = 1111,
) -> Tuple[float, float, float, int]:
    """
    Compute a paired metric difference and percentile bootstrap CI.

    The same resampled patients are used for both strategies in each bootstrap
    replicate. Sampling is stratified by outcome when both classes are present.
    """

    y_true = _as_1d_binary_array(
        y_true
    )

    p_first = _as_1d_float_array(
        p_first
    )

    p_second = _as_1d_float_array(
        p_second
    )

    if not (
        len(y_true)
        == len(p_first)
        == len(p_second)
    ):
        raise ValueError(
            "y_true, p_first, and p_second "
            "must have the same length."
        )

    n = len(y_true)

    if n == 0:
        raise ValueError(
            "Cannot bootstrap an empty prediction set."
        )

    estimate = paired_metric_difference(
        y_true,
        p_first,
        p_second,
        metric,
    )

    rng = np.random.default_rng(
        random_state
    )

    positive_idx = np.where(
        y_true == 1
    )[0]

    negative_idx = np.where(
        y_true == 0
    )[0]

    boot_differences = []

    if (
        len(positive_idx) > 0
        and len(negative_idx) > 0
    ):
        for _ in range(n_boot):
            boot_positive = rng.choice(
                positive_idx,
                size=len(positive_idx),
                replace=True,
            )

            boot_negative = rng.choice(
                negative_idx,
                size=len(negative_idx),
                replace=True,
            )

            boot_idx = np.concatenate(
                [
                    boot_positive,
                    boot_negative,
                ]
            )

            difference = paired_metric_difference(
                y_true[boot_idx],
                p_first[boot_idx],
                p_second[boot_idx],
                metric,
            )

            if not pd.isna(difference):
                boot_differences.append(
                    difference
                )

    elif str(metric).lower() == "brier":
        base_idx = np.arange(n)

        for _ in range(n_boot):
            boot_idx = rng.choice(
                base_idx,
                size=n,
                replace=True,
            )

            difference = paired_metric_difference(
                y_true[boot_idx],
                p_first[boot_idx],
                p_second[boot_idx],
                metric,
            )

            boot_differences.append(
                difference
            )

    else:
        return (
            estimate,
            float("nan"),
            float("nan"),
            0,
        )

    if not boot_differences:
        return (
            estimate,
            float("nan"),
            float("nan"),
            0,
        )

    alpha = 1.0 - ci_level

    ci_lower = float(
        np.percentile(
            boot_differences,
            100 * alpha / 2,
        )
    )

    ci_upper = float(
        np.percentile(
            boot_differences,
            100 * (1 - alpha / 2),
        )
    )

    return (
        estimate,
        ci_lower,
        ci_upper,
        len(boot_differences),
    )


# =============================================================================
# Generic one-comparison helper
# =============================================================================

def _compute_one_comparison(
    *,
    site_id,
    first_metrics_df: pd.DataFrame,
    first_metrics_csv: Path,
    first_model_type: str,
    first_dataset_name: str,
    first_strategy: str,
    second_metrics_df: pd.DataFrame,
    second_metrics_csv: Path,
    second_model_type: str,
    second_dataset_name: str,
    second_strategy: str,
    comparison_label: str,
    setting=None,
    n_boot: int = 1000,
    ci_level: float = 0.95,
    random_state: int = 1111,
) -> Dict:

    (
        y_first,
        p_first,
        first_prediction_path,
    ) = _get_prediction_for_site_model(
        metrics_df=first_metrics_df,
        metrics_csv=first_metrics_csv,
        site_id=site_id,
        model_type=first_model_type,
        dataset_name=first_dataset_name,
    )

    (
        y_second,
        p_second,
        second_prediction_path,
    ) = _get_prediction_for_site_model(
        metrics_df=second_metrics_df,
        metrics_csv=second_metrics_csv,
        site_id=site_id,
        model_type=second_model_type,
        dataset_name=second_dataset_name,
    )

    _check_same_patients(
        y_first,
        y_second,
        context=(
            f"Site {site_id}, "
            f"{comparison_label}"
        ),
    )

    (
        auc_difference,
        auc_ci_lower,
        auc_ci_upper,
        auc_boot_n,
    ) = paired_stratified_bootstrap_difference_ci(
        y_first,
        p_first,
        p_second,
        metric="auc",
        n_boot=n_boot,
        ci_level=ci_level,
        random_state=random_state,
    )

    (
        brier_difference,
        brier_ci_lower,
        brier_ci_upper,
        brier_boot_n,
    ) = paired_stratified_bootstrap_difference_ci(
        y_first,
        p_first,
        p_second,
        metric="brier",
        n_boot=n_boot,
        ci_level=ci_level,
        random_state=random_state,
    )

    row = {
        "site_id": site_id,
        "comparison": comparison_label,
        "first_strategy": first_strategy,
        "second_strategy": second_strategy,
        "n": int(len(y_first)),
        "n_positive": int(
            np.sum(y_first == 1)
        ),
        "n_negative": int(
            np.sum(y_first == 0)
        ),
        "auc_difference": auc_difference,
        "auc_ci_lower": auc_ci_lower,
        "auc_ci_upper": auc_ci_upper,
        "auc_bootstrap_replicates_used": auc_boot_n,
        "brier_difference": brier_difference,
        "brier_ci_lower": brier_ci_lower,
        "brier_ci_upper": brier_ci_upper,
        "brier_bootstrap_replicates_used": brier_boot_n,
        "first_prediction_csv": str(
            first_prediction_path
        ),
        "second_prediction_csv": str(
            second_prediction_path
        ),
    }

    if setting is not None:
        row["setting"] = setting

    return row


# =============================================================================
# Primary paired within-site differences
# =============================================================================

def compute_paired_within_site_differences(
    *,
    primary_metrics_csv,
    dataset_name: str = PRIMARY_DATASET_NAME,
    n_boot: int = 1000,
    ci_level: float = 0.95,
    random_state: int = 1111,
) -> pd.DataFrame:
    """
    Compute the standard paired within-site comparisons:

        FL - local
        FL - centralised
        Centralised - local
    """

    primary_metrics_csv = Path(
        primary_metrics_csv
    )

    if not primary_metrics_csv.exists():
        raise FileNotFoundError(
            f"Primary metrics CSV not found: "
            f"{primary_metrics_csv}"
        )

    primary_df = pd.read_csv(
        primary_metrics_csv
    )

    site_col = _get_site_column(
        primary_df
    )

    site_ids = sorted(
        primary_df[
            site_col
        ]
        .dropna()
        .unique(),
        key=_site_sort_key,
    )

    comparisons = [
        {
            "comparison": "FL - local",
            "first_model_type": FL_MODEL_TYPE,
            "second_model_type": LOCAL_MODEL_TYPE,
            "first_strategy": "FL",
            "second_strategy": "Local",
        },
        {
            "comparison": "FL - centralised",
            "first_model_type": FL_MODEL_TYPE,
            "second_model_type": CENTRAL_MODEL_TYPE,
            "first_strategy": "FL",
            "second_strategy": "Centralised",
        },
        {
            "comparison": "Centralised - local",
            "first_model_type": CENTRAL_MODEL_TYPE,
            "second_model_type": LOCAL_MODEL_TYPE,
            "first_strategy": "Centralised",
            "second_strategy": "Local",
        },
    ]

    rows = []

    for site_id in site_ids:
        for comparison in comparisons:
            rows.append(
                _compute_one_comparison(
                    site_id=site_id,
                    first_metrics_df=primary_df,
                    first_metrics_csv=primary_metrics_csv,
                    first_model_type=comparison[
                        "first_model_type"
                    ],
                    first_dataset_name=dataset_name,
                    first_strategy=comparison[
                        "first_strategy"
                    ],
                    second_metrics_df=primary_df,
                    second_metrics_csv=primary_metrics_csv,
                    second_model_type=comparison[
                        "second_model_type"
                    ],
                    second_dataset_name=dataset_name,
                    second_strategy=comparison[
                        "second_strategy"
                    ],
                    comparison_label=comparison[
                        "comparison"
                    ],
                    n_boot=n_boot,
                    ci_level=ci_level,
                    random_state=random_state,
                )
            )

    results = pd.DataFrame(
        rows
    )

    if not results.empty:
        comparison_order = {
            "FL - local": 0,
            "FL - centralised": 1,
            "Centralised - local": 2,
        }

        results["_site_sort"] = (
            pd.to_numeric(
                results["site_id"],
                errors="coerce",
            )
        )

        results["_comparison_order"] = (
            results["comparison"]
            .map(
                comparison_order
            )
        )

        results = (
            results
            .sort_values(
                [
                    "_site_sort",
                    "_comparison_order",
                ]
            )
            .drop(
                columns=[
                    "_site_sort",
                    "_comparison_order",
                ]
            )
            .reset_index(
                drop=True
            )
        )

    return results


# =============================================================================
# Fidelity / transportability
# =============================================================================

def compute_fidelity_recovery_differences(
    *,
    primary_metrics_csv,
    leave_site_out_metrics_csv,
    primary_dataset_name: str = PRIMARY_DATASET_NAME,
    leave_site_out_dataset_name: str = LEAVE_SITE_OUT_DATASET_NAME,
    n_boot: int = 1000,
    ci_level: float = 0.95,
    random_state: int = 1111,
) -> pd.DataFrame:
    """
    Compare shared models with the local model under within-network and
    leave-site-out settings.

    Comparisons:
        Within-network:
            FL - local
            Centralised - local

        Leave-site-out:
            FL - local
            Centralised - local
    """

    primary_metrics_csv = Path(
        primary_metrics_csv
    )

    leave_site_out_metrics_csv = Path(
        leave_site_out_metrics_csv
    )

    if not primary_metrics_csv.exists():
        raise FileNotFoundError(
            f"Primary metrics CSV not found: "
            f"{primary_metrics_csv}"
        )

    if not leave_site_out_metrics_csv.exists():
        raise FileNotFoundError(
            f"Leave-site-out metrics CSV not found: "
            f"{leave_site_out_metrics_csv}"
        )

    primary_df = pd.read_csv(
        primary_metrics_csv
    )

    leave_site_out_df = pd.read_csv(
        leave_site_out_metrics_csv
    )

    lso_site_col = _get_site_column(
        leave_site_out_df
    )

    site_ids = sorted(
        leave_site_out_df[
            lso_site_col
        ]
        .dropna()
        .unique(),
        key=_site_sort_key,
    )

    rows = []

    for site_id in site_ids:

        # Within-network FL - local
        rows.append(
            _compute_one_comparison(
                site_id=site_id,
                first_metrics_df=primary_df,
                first_metrics_csv=primary_metrics_csv,
                first_model_type=FL_MODEL_TYPE,
                first_dataset_name=primary_dataset_name,
                first_strategy="FL",
                second_metrics_df=primary_df,
                second_metrics_csv=primary_metrics_csv,
                second_model_type=LOCAL_MODEL_TYPE,
                second_dataset_name=primary_dataset_name,
                second_strategy="Local",
                comparison_label="FL - local",
                setting="Within-network",
                n_boot=n_boot,
                ci_level=ci_level,
                random_state=random_state,
            )
        )

        # Within-network centralised - local
        rows.append(
            _compute_one_comparison(
                site_id=site_id,
                first_metrics_df=primary_df,
                first_metrics_csv=primary_metrics_csv,
                first_model_type=CENTRAL_MODEL_TYPE,
                first_dataset_name=primary_dataset_name,
                first_strategy="Centralised",
                second_metrics_df=primary_df,
                second_metrics_csv=primary_metrics_csv,
                second_model_type=LOCAL_MODEL_TYPE,
                second_dataset_name=primary_dataset_name,
                second_strategy="Local",
                comparison_label="Centralised - local",
                setting="Within-network",
                n_boot=n_boot,
                ci_level=ci_level,
                random_state=random_state,
            )
        )

        # Leave-site-out FL - local
        rows.append(
            _compute_one_comparison(
                site_id=site_id,
                first_metrics_df=leave_site_out_df,
                first_metrics_csv=leave_site_out_metrics_csv,
                first_model_type=LSO_FL_MODEL_TYPE,
                first_dataset_name=leave_site_out_dataset_name,
                first_strategy="FL leave-site-out",
                second_metrics_df=primary_df,
                second_metrics_csv=primary_metrics_csv,
                second_model_type=LOCAL_MODEL_TYPE,
                second_dataset_name=primary_dataset_name,
                second_strategy="Local",
                comparison_label="FL - local",
                setting="Leave-site-out",
                n_boot=n_boot,
                ci_level=ci_level,
                random_state=random_state,
            )
        )

        # Leave-site-out centralised - local
        rows.append(
            _compute_one_comparison(
                site_id=site_id,
                first_metrics_df=leave_site_out_df,
                first_metrics_csv=leave_site_out_metrics_csv,
                first_model_type=LSO_CENTRAL_MODEL_TYPE,
                first_dataset_name=leave_site_out_dataset_name,
                first_strategy="Centralised leave-site-out",
                second_metrics_df=primary_df,
                second_metrics_csv=primary_metrics_csv,
                second_model_type=LOCAL_MODEL_TYPE,
                second_dataset_name=primary_dataset_name,
                second_strategy="Local",
                comparison_label="Centralised - local",
                setting="Leave-site-out",
                n_boot=n_boot,
                ci_level=ci_level,
                random_state=random_state,
            )
        )

    results = pd.DataFrame(
        rows
    )

    if not results.empty:
        setting_order = {
            "Within-network": 0,
            "Leave-site-out": 1,
        }

        comparison_order = {
            "FL - local": 0,
            "Centralised - local": 1,
        }

        results["_site_sort"] = (
            pd.to_numeric(
                results["site_id"],
                errors="coerce",
            )
        )

        results["_setting_order"] = (
            results["setting"]
            .map(
                setting_order
            )
        )

        results["_comparison_order"] = (
            results["comparison"]
            .map(
                comparison_order
            )
        )

        results = (
            results
            .sort_values(
                [
                    "_site_sort",
                    "_setting_order",
                    "_comparison_order",
                ]
            )
            .drop(
                columns=[
                    "_site_sort",
                    "_setting_order",
                    "_comparison_order",
                ]
            )
            .reset_index(
                drop=True
            )
        )

    return results


# =============================================================================
# Run and save
# =============================================================================

def run_paired_comparison_analysis(
    *,
    primary_metrics_csv,
    output_dir,
    leave_site_out_metrics_csv=None,
    n_boot: int = 1000,
    ci_level: float = 0.95,
    random_state: int = 1111,
    verbose: bool = True,
):
    """
    Run and save paired comparison analyses

    The primary paired within-site comparison is always generated. Fidelity /
    transportability comparisons are additionally generated when a leave-site-
    out metrics CSV is provided.
    """

    output_dir = Path(
        output_dir
    )

    output_dir.mkdir(
        parents=True,
        exist_ok=True,
    )

    # -------------------------------------------------------------------------
    # Primary paired within-site differences
    # -------------------------------------------------------------------------

    paired_within_site_df = (
        compute_paired_within_site_differences(
            primary_metrics_csv=primary_metrics_csv,
            n_boot=n_boot,
            ci_level=ci_level,
            random_state=random_state,
        )
    )

    paired_within_site_out = (
        output_dir
        / "paired_within_site_differences.csv"
    )

    paired_within_site_df.to_csv(
        paired_within_site_out,
        index=False,
    )

    # -------------------------------------------------------------------------
    # Fidelity / transportability
    # -------------------------------------------------------------------------

    if leave_site_out_metrics_csv is not None:
        fidelity_recovery_df = (
            compute_fidelity_recovery_differences(
                primary_metrics_csv=primary_metrics_csv,
                leave_site_out_metrics_csv=leave_site_out_metrics_csv,
                n_boot=n_boot,
                ci_level=ci_level,
                random_state=random_state,
            )
        )

        fidelity_recovery_out = (
            output_dir
            / "fidelity_recovery_differences.csv"
        )

        fidelity_recovery_df.to_csv(
            fidelity_recovery_out,
            index=False,
        )

    else:
        fidelity_recovery_df = (
            pd.DataFrame()
        )

        fidelity_recovery_out = None

    if verbose:
        print(
            "\nPaired within-site differences saved to:"
            f"\n{paired_within_site_out}"
        )

        if fidelity_recovery_out is not None:
            print(
                "\nFidelity / transportability differences saved to:"
                f"\n{fidelity_recovery_out}"
            )

    return {
        "paired_within_site_df":
            paired_within_site_df,

        "paired_within_site_out":
            paired_within_site_out,

        "fidelity_recovery_df":
            fidelity_recovery_df,

        "fidelity_recovery_out":
            fidelity_recovery_out,
    }